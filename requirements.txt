Flask==2.3.2                # Web framework to handle routing and templating
pandas==1.6.0               # (Optional) For any data analysis or tabular data manipulation
numpy==1.24.3               # (Optional) For numerical calculations (if needed)
Jinja2==3.1.2               # Template engine used by Flask (usually bundled with Flask)
Flask-WTF==1.0.1            # (Optional) For form handling and validation in Flask
Werkzeug==2.3.2             # Flask's underlying library for HTTP utilities (typically installed with Flask)
